import 'package:test/test.dart';

///
void main() {
  group('', () {
    setUp(() {});

    tearDown(() {});

    test('basic', () {
      // set up
//      var done = expectAsync(() {});
      // exercise
      // verification
      expect('', equals(''));
      // tear down
    });
  });
}
